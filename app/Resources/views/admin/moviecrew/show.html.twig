{% extends 'admin/admin_layout.html.twig' %}

{% block body %}
    <h1>Moviecrew</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movieCrew.id }}</td>
            </tr>
        </tbody>
    </table>

<hr>

    <ul class="list-unstyled list-inline">
        <li class="list-inline-item">
            <a class="btn btn-success" href="{{ path('moviecrew_index') }}">Back to the list</a>
        </li>
        <li class="list-inline-item">
            <a class="btn btn-primary" href="{{ path('moviecrew_edit', { 'id': movieCrew.id }) }}">Edit</a>
        </li>
        <li class="list-inline-item">
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{% extends 'admin/admin_layout.html.twig' %}

{% block body %}

<h1> Secteur d'activité </h1>
<p>
    <a href="{{ path('department_add')}}" class="btn btn-success btn-sm"> Ajouter <a>
<p>
<table class="table table-striped table-hover">
    <thead>
        <th> Secteur </th>
        <th></th>
        <th></th>
    </thead>
    <tbody>
    {% for d in departments %}
        <tr>
            <td>
                <a href="{{ path('department_show', { "id" : d.id } )}}">{{ d.name }}</a>
            </td>
            <td>
                <a href="{{ path('department_edit', { "id" : d.id } )}}" class="btn btn-primary btn-sm"> Modifier </a>
            </td>
            <td>
                <a href="{{ path('department_delete', { "id" : d.id } )}}" class="btn btn-danger btn-sm"> Supprimer </a>
            </td>  
        </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}